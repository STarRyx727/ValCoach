[package]
name = "valcoach-metrics"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
valcoach-domain = { path = "../domain" }
